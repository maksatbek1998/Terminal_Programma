<Window x:Class="Terminal.Window.Kategory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Terminal.Window"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200" WindowState="Maximized" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Grid>
        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Top"> 
            <Button Style="{DynamicResource Menu_Button}" Content="Прием к врачу" Height="80" Width="500"/>
            <Button Style="{DynamicResource Menu_Button}" Content="Прием к врачу" Height="80" Width="500"/>
            <Button Style="{DynamicResource Menu_Button}" Content="Прием к врачу" Height="80" Width="500"/>
            <Button Style="{DynamicResource Menu_Button}" Content="Прием к врачу" Height="80" Width="500"/>
            <Button Style="{DynamicResource Menu_Button}" Content="Прием к врачу" Height="80" Width="500"/>

        </WrapPanel>
    </Grid>
</Window>
